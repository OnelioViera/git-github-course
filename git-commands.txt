Git Commands

Create Git repositories via: git init (Git commands require a repository in a project. Only requires once per folder/project)

Steps taken to add a file.
1. git add "plus file name"
2. git status "gives current status of reposatory"
3. git commit -m "Some text"
4. get log
5. git checkout "plus commit id"
6. git checkout master